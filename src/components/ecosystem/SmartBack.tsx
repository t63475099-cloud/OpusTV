"use client";

/**
 * Nút Quay lại trang xem phim đã chuyển vào Navbar sticky
 * (không trôi theo scroll / Lenis). Component giữ để layout không gãy import.
 */
export default function SmartBack() {
  return null;
}
